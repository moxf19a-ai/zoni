# ==========================================================================
# apps/web environment variables.
# Copy this file to `.env` inside apps/web/ and adjust as needed.
# Vite only exposes variables prefixed with VITE_ to client-side code.
# ==========================================================================

VITE_API_BASE_URL=http://localhost:4000
